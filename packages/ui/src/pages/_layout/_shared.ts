import { css } from '@emotion/react'

export const sharedPageCss = {
  page: css`
    height: calc(100vh - 46px);
    padding: 10px 0;
    display: flex;
    flex-direction: column;
    overflow-y: hidden;
  `,
}
